import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Keep the docs/ folder as a fallback static directory during transition
  // Static assets (fonts, images) are served from public/
};

export default nextConfig;
